---
clusterName: "opensearch-cluster"

securityConfig:
  data: {}
    # config.yml: |-
    # internal_users.yml: |-
    # roles.yml: |-
    # roles_mapping.yml: |-
    # action_groups.yml: |-
    # tenants.yml: |-

tls:
  enabled: false
  issuer: {}

# Used for autocreating ism policy and index-template
indexName: filebeat

ism:
  rollover:
    period: 20h
  delete:
    period: 30d